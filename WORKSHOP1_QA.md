# สรุปประวัติการพูดคุย Workshop #1 (Q&A Log)

---

## ❓ คำถามที่ 1
> **ผู้เรียน:** อธิบายงาน workshop1 คืออะไร ต้องทำอะไรบ้าง

### 💡 คำตอบจาก AI

#### 📌 ภาพรวม Workshop #1
ไฟล์ `src/main/kotlin/Workshop1.kt` คือการเขียน **Simple Console Application: Unit Converter** (โปรแกรมแปลงหน่วยผ่าน Console)

* **โจทย์:** แสดงเมนู → ผู้ใช้เลือกเมนู → ป้อนตัวเลข → โปรแกรมแปลงหน่วยแล้วแสดงผล → วนกลับไปเมนูใหม่จนกว่าจะพิมพ์ `exit`
* **โครงสร้าง:** มีโครงหลักเขียนไว้ให้แล้ว ส่วนที่มีเครื่องหมาย 🚨 คือช่องว่างที่ต้องเติม มีทั้งหมด 6 จุด

---

#### 📋 สิ่งที่ต้องทำ (แบ่งตามหมายเลขในไฟล์)

| # | บรรทัด | สิ่งที่ต้องทำ | แนวคิดที่ฝึก |
|---|--------|--------------|--------------|
| **1–2** | 7–17 | ✅ มีให้แล้ว (เมนู + `while(true)` + `readln()`) | Loop, Console I/O |
| **3** | 26–31 | เขียน `when` เพื่อแตกเส้นทางตาม `choice`<br>- `"1"` → เรียก `convertCelsiusToFahrenheit()`<br>- `"2"` → เรียก `convertKilometersToMiles()`<br>- `"exit"` → ออกจากโปรแกรม<br>- อื่นๆ → แจ้งเตือนผู้ใช้ว่าเลือกผิด | `when` expression / statement |
| **4** | 40, 47 | สร้างฟังก์ชันแปลงหน่วย 2 ตัว:<br>- `celsiusToFahrenheit(celsius: Double): Double`<br>- `kilometersToMiles(kilo: Double): Double`<br>*(สูตรเขียนไว้ในคอมเมนต์แล้ว)* | Function, Expression body |
| **5** | 59, 75 | แปลง `input` (String) → `Double` แบบปลอดภัย ถ้าแปลงไม่ได้ให้ `return` ออกจากฟังก์ชัน | Null Safety (`toDoubleOrNull()` + Elvis `?:`) |
| **6** | 64, 79 | ปลดคอมเมนต์บรรทัดแสดงผลลัพธ์ พิมพ์ผลลัพธ์ด้วย String Format ทศนิยม 2 ตำแหน่ง | String Template, `"%.2f".format()` |

---

#### 🧠 จุดที่ควรคิดล่วงหน้า
1. **ข้อ 3 — `when` กับการออกจากโปรแกรม:**
   - `while(true)` ไม่มีทางจบเอง ต้องหาวิธีหยุดโปรแกรมเมื่อพิมพ์ `exit` (เช่น ใช้ `exitProcess(0)`)
2. **ข้อ 5 — Elvis operator `?:`:**
   - รูปแบบ: `นิพจน์ที่อาจเป็น null ?: ค่าสำรอง`
   - ฝั่งขวาของ `?:` ไม่จำเป็นต้องเป็น "ค่า" เสมอไป สามารถใส่ `return` ได้ เพราะใน Kotlin `return` มี Type เป็น `Nothing` ซึ่งเข้ากันได้กับทุก Type
3. **ข้อ 4 — ทำไมต้อง `9.0 / 5.0` ไม่ใช่ `9 / 5`:**
   - ใน Kotlin `9 / 5` เป็น Integer Division จะได้ผลลัพธ์เป็น `1` (ทศนิยมถูกตัดทิ้ง) จึงต้องใช้ `9.0 / 5.0` เพื่อให้ได้ค่า `1.8`

---

#### 🚀 วิธีการรันโปรแกรม
```bash
./gradlew run
```

---

## ❓ คำถามที่ 2
> **ผู้เรียน:** ช่วย test workshop1 ให้หน่อย

### 💡 คำตอบจาก AI

#### 🧪 1. ผลการรัน Unit Test (`./gradlew clean test`)

```text
BUILD SUCCESSFUL
4 tests, 0 failures
```

> ⚠️ **ข้อควรระวัง:** ใน `WorkshopTest.kt` มี Test 4 ตัว แต่ 3 ตัวมีบอร์ดี้ว่างเปล่า (ผ่านเพราะไม่ได้ `assert` อะไรเลย)
>
> | Test Name | สถานะจริง |
> |-----------|-----------|
> | `test celsiusToFahrenheit with positive value` | ✅ ทดสอบจริง (20°C → 68°F) |
> | `test celsiusToFahrenheit with zero` | ⬜ ว่างเปล่า (ต้องเขียนเพิ่ม) |
> | `test celsiusToFahrenheit with negative value` | ⬜ ว่างเปล่า (ต้องเขียนเพิ่ม) |
> | `test kilometersToMiles with one kilometer` | ⬜ ว่างเปล่า (ต้องเขียนเพิ่ม) |

---

#### 🖥️ 2. ผลการรันโปรแกรมจริง (ทดลองป้อน Input)

| Input ที่ป้อน | ผลลัพธ์ที่ได้ | สถานะ |
|---------------|---------------|--------|
| `1` → `20` | `20.0 °C เท่ากับ 68.00 °F` | ✅ ทำงานถูกต้อง |
| `2` → `1` | `1.0 km เท่ากับ 0.62 miles` | ✅ ทำงานถูกต้อง |
| `1` → `abc` | เงียบและกลับไปหน้าเมนู | ⚠️ ไม่ crash แต่ไม่มีข้อความแจ้งผู้ใช้ |
| `9` | ไม่มีข้อความตอบรับ | 🐛 เลือกเมนูผิดแต่ไม่แจ้งเตือน |
| `exit` | ออกจากโปรแกรม | ✅ ทำงานถูกต้อง |

---

#### 💡 ประเด็นคำถามชวนคิดต่อ

สังเกตบรรทัดที่ 30:
```kotlin
else -> "Invalid Choice"
```
* **คำใบ้:** ใน Kotlin `when` เป็นได้ทั้ง Statement และ Expression
  * `"1" -> convertCelsiusToFahrenheit()` *(เป็นการกระทำ)*
  * `else -> "Invalid Choice"` *(เป็นการคืนค่า String)*
* ลองคิดดูว่าค่า `"Invalid Choice"` ที่ได้จาก `when` ถูกนำไปเก็บหรือใช้งานต่อที่ไหนหรือไม่? หากไม่มีใครรับค่านั้น ข้อความก็จะไม่ถูกพิมพ์ออกทาง Console
